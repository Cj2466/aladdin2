# Local dev default matches the backend's default port; leave unset to use
# it. Production sets this as a Cloudflare Pages build environment
# variable instead of a committed file, pointing at the Render backend URL.
VITE_API_BASE_URL=http://localhost:8000
